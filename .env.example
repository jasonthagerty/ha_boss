# HA Boss Environment Configuration
# This file works for both local development and Docker deployment
# For Docker: Copy to .env in the project root
# For local: Copy to config/.env

# Home Assistant Configuration
# Required: Your Home Assistant instance URL
HA_URL=http://homeassistant.local:8123

# Required: Long-lived access token from HA
# Create at: http://your-ha/profile -> Long-Lived Access Tokens
HA_TOKEN=your_long_lived_access_token_here

# Optional: Email notifications
# EMAIL_PASSWORD=your_app_password_here

# Optional: Timezone
# TZ=America/New_York

# Optional: API Server Port (default: 8000)
# API_PORT=8000

# Development Settings
# LOG_LEVEL=DEBUG
# MODE=dry_run

# Testing (for running tests against a test HA instance)
# TEST_HA_URL=http://localhost:8123
# TEST_HA_TOKEN=test_token_here

# Future: AI Features (Phase 2+)
# ANTHROPIC_API_KEY=your_claude_api_key_here
# OLLAMA_URL=http://localhost:11434

# MCP Server Configuration (optional - only if using MCP server)
# API key for MCP server to access HA Boss API (if auth enabled)
# HABOSS_API_KEY=your_api_key_here

# MCP transport mode: stdio (default), http, or sse
# MCP_TRANSPORT=stdio

# MCP OAuth authentication (for remote access)
# MCP_AUTH_ENABLED=false
# MCP_AUTH_CLIENT_ID=your_oauth_client_id
# MCP_AUTH_CLIENT_SECRET=your_oauth_client_secret
# MCP_AUTH_BASE_URL=https://your-domain.com
